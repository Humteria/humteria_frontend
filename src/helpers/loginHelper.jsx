export async function login(mail, password) {
  //create endpoint for login (post)
}

export async function getLoggedInUser() {
  const token = localStorage.getItem("token");
  //create enpoint to get loggedInUser (get)
}
